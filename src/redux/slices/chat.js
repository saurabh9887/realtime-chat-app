import { createSlice } from '@reduxjs/toolkit';


export const chatSlice = createSlice({
    name: 'chat',
    initialState: {},
    reducers: {
        publishMessage: (state, action) => {

        },
        subscribeMessage: (state, action) => {
            
        }
    }
});
